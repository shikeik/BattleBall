# 检查清单 v0.2.4

## 概述

基于实际代码分析，重构检查清单部分过时。本版本聚焦：
- A: GeometryFactory 提取（消除重复 switch）
- B: 魔法数字清理（提取硬编码到 CONFIG）

---

## 阶段 A：GeometryFactory 提取

### A.1 CONFIG 新增几何体配置

- [x] 在 `CONFIG` 中添加 `GEOMETRY` 配置对象
- [x] 定义 `cone` 几何体（body + shell + rotation）
- [x] 定义 `sphere` 几何体（body + shell）
- [x] 定义 `octahedron` 几何体（body + shell）
- [x] 定义 `box` 默认几何体

### A.2 重构 createEnemyBody()

- [x] 使用 `CONFIG.GEOMETRY[cfg.geometry].body(size)` 创建几何体
- [x] 应用配置的 rotation
- [x] 删除原有的 switch 语句
- [ ] 验证三种敌人类型外观正常

### A.3 重构 createEnemyShell()

- [x] 使用 `CONFIG.GEOMETRY[cfg.geometry].shell(size)` 创建几何体
- [x] 删除原有的 switch 语句
- [ ] 验证外壳效果正常

### A.4 清理废弃代码

- [x] 删除 `getEnemyShellGeometry()` 方法（第 236-247 行）
- [x] 验证无引用错误

### A.5 验证

- [ ] 游戏启动无报错
- [ ] 三种敌人类型生成正常（chaser/drone/heavy）
- [ ] 敌人外观与重构前一致
- [ ] 外壳（wireframe）显示正常

**提交**: `13f748e` - refactor: 提取 GeometryFactory，消除重复 switch

---

## 阶段 B：魔法数字清理

### B.1 CONFIG 新增碰撞配置

```javascript
COLLISION: {
    BULLET_RADIUS: 2.5,
    PLAYER_RADIUS: 3
}
```

- [x] 添加 `CONFIG.COLLISION` 对象
- [x] 定义 `BULLET_RADIUS: 2.5`
- [x] 定义 `PLAYER_RADIUS: 3`

### B.2 CONFIG 扩展 ENEMY 配置

```javascript
ENEMY: {
    // ... 现有配置 ...
    SPAWN_RANGE_X: 40,           // 生成范围 ±20
    SPAWN_DISTANCE_Z: 150,       // 前方生成距离
    TRACKING_FACTOR: 0.3,        // 追踪速度系数
    DESPAWN_DISTANCE_NEAR: 20,   // 近处销毁距离
    DESPAWN_DISTANCE_FAR: -300   // 远处销毁距离
}
```

- [x] 添加 `SPAWN_RANGE_X: 40`
- [x] 添加 `SPAWN_DISTANCE_Z: 150`
- [x] 添加 `TRACKING_FACTOR: 0.3`
- [x] 添加 `DESPAWN_DISTANCE_NEAR: 20`
- [x] 添加 `DESPAWN_DISTANCE_FAR: -300`

### B.3 替换 game.js 魔法数字

| 行号 | 原值 | 替换为 |
|------|------|--------|
| 161 | `40` | `CONFIG.ENEMY.SPAWN_RANGE_X` |
| 163 | `150` | `CONFIG.ENEMY.SPAWN_DISTANCE_Z` |
| 263 | `2.5` | `CONFIG.COLLISION.BULLET_RADIUS` |
| 270 | `3` | `CONFIG.COLLISION.PLAYER_RADIUS` |

- [x] 替换第 161 行
- [x] 替换第 163 行
- [x] 替换第 263 行
- [x] 替换第 270 行

### B.4 替换 enemy-helper.js 魔法数字

| 行号 | 原值 | 替换为 |
|------|------|--------|
| 24 | `0.3` | `CONFIG.ENEMY.TRACKING_FACTOR` |
| 31 | `20` | `CONFIG.ENEMY.DESPAWN_DISTANCE_NEAR` |
| 31 | `-300` | `CONFIG.ENEMY.DESPAWN_DISTANCE_FAR` |

- [x] 替换第 24 行
- [x] 替换第 31 行

### B.5 验证

- [ ] 游戏启动无报错
- [ ] 敌人生成位置正常
- [ ] 子弹碰撞检测正常
- [ ] 玩家碰撞检测正常
- [ ] 敌人追踪速度正常
- [ ] 敌人生命周期正常

**提交**: `dc1c522` - refactor: 清理魔法数字，提取到 CONFIG

---

## 提交记录

| 阶段 | 提交信息 | 验证状态 |
|------|----------|----------|
| A.1-A.5 | refactor: 提取 GeometryFactory，消除重复 switch | ✅ 已提交 |
| B.1-B.5 | refactor: 清理魔法数字，提取到 CONFIG | ✅ 已提交 |

---

## 代码统计

| 指标 | 重构前 | 重构后 | 变化 |
|------|--------|--------|------|
| entities.js 行数 | 822 | 777 | -45 ✅ |
| config.js 行数 | 106 | 143 | +37 ✅ |
| 重复 switch 数量 | 2 | 0 | -2 ✅ |
| 硬编码数量 | 7 | 0 | -7 ✅ |

---

## 紧急回滚

如出现问题：
```bash
git reset --hard <上一提交哈希>
```
